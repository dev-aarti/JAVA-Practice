// javaScript method toString() converts an array to a string of array values.
const fruits = ["Apple", "Banana", "Grapes", "Mango", "Cucumber", "Orange", "Kivi"];
document.getElementById("demo").innerHTML = fruits.toString();

// WE can also creates arrays using new keyword.
const cars = new  Array("Saas", "Volvo", "BMW");

// access the full array
console.log(cars);